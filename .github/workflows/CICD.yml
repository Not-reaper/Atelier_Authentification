name: Industrialisation continue sur le serveur Alwaysdata

on:
  push:
    branches: [ "main" ]

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Déploiement et Configuration Sécurité
        uses: appleboy/ssh-action@master
        with:
          host: "ssh-${{ secrets.USERNAME }}.alwaysdata.net"
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          envs: 
            PASS_CLAIR: ${{ secrets.HTPASSWD_CONTENT }}
          script: |
            REPO_URL="https://github.com/${{ github.repository }}.git"
            TARGET_DIR="$HOME/www"
            CLONE_DIR="$HOME/clone_temp"

            # --- 1. CLONAGE DU SITE ---
            rm -rf $CLONE_DIR
            git clone $REPO_URL $CLONE_DIR

            # --- 2. DEPLOIEMENT ---
            mkdir -p $TARGET_DIR
            if [ -d "$CLONE_DIR" ]; then
              rsync -r --delete --exclude '.git' $CLONE_DIR/ $TARGET_DIR/
              rm -rf $CLONE_DIR
              echo "✅ Site mis à jour."
            else
              echo "❌ Erreur de clonage."
              exit 1
            fi

            # --- 3. SECURITE ---
            echo "🔒 Génération du fichier mot de passe..."
            mkdir -p $TARGET_DIR/atelier1
            rm -f $TARGET_DIR/atelier1/.htpasswd

            # --- Vérifier si htpasswd est installé ---
            if ! command -v htpasswd &> /dev/null; then
                echo "htpasswd non trouvé, installation..."
                sudo apt-get update && sudo apt-get install -y apache2-utils
            fi

            # Crée le fichier .htpasswd avec admin / PASS_CLAIR
            htpasswd -cb $TARGET_DIR/atelier1/.htpasswd admin "$PASS_CLAIR"

            # Vérification finale
            if [ -f "$TARGET_DIR/atelier1/.htpasswd" ]; then
                echo "✅ Fichier .htpasswd généré avec succès."
                ls -l $TARGET_DIR/atelier1/.htpasswd
            else
                echo "❌ Echec de la création du fichier mot de passe."
                exit 1
            fi
